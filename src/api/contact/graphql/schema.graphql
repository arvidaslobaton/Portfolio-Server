type Mutation {
  sendContactEmail(
    firstName: String!
    lastName: String!
    email: String!
    message: String!
    phone: String
  ): ContactResponse!
}

type ContactResponse {
  success: Boolean!
  message: String!
}
